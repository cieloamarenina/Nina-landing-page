# n8n instance
N8N_BASE_URL=https://n8n.ninalearnsvibecoding.com
N8N_API_KEY=

# JWT secret — must match services/auth/.env JWT_SECRET
JWT_SECRET=

# CORS allowed origin
ALLOWED_ORIGIN=https://ninalearnsvibecoding.com

# Flask
FLASK_PORT=5001
